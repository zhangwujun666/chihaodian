<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yq.dao.UserSettingDao">
    <insert id="insert" parameterType="java.util.Map">
	insert tb_user(share_point,order_point,money_point,one_start,one_end,two_start,two_end,three_start,three_end,four_start,four_end,fiv_start,five_end)
	values
	(
	#{share_point},#{order_point},#{money_point},#{one_start},#{one_end},#{two_start},#{two_end}，#{three_start}，#{three_end}，#{four_start}，#{four_end}，#{fiv_start}，#{five_end}
	) 
    </insert>
    
    <update id="update" parameterType="java.util.Map">
    <!--update tb_user set-->
    <!--realname=#{realname},-->
    <!--head_img=#{head_img}-->
    <!--where oppen_id=#{oppen_id}-->
    </update>
</mapper>